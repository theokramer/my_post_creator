services:
  - type: web
    name: post-creator
    env: docker
    plan: starter
    disk:
      name: sqlite-data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: DATABASE_PATH
        value: /data/data.db
      - key: PORT
        value: 3000
